import { View, Text } from 'react-native'

export default function FlowScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-black">
      <Text className="text-white text-3xl">🌀 Flow Mode</Text>
    </View>
  )
}
